import { StudentEdit, StudentCreate } from "./form";
import { StudentList } from "./list";

export const students = {
  name: "students",
  list: StudentList,
  edit: StudentEdit,
  create: StudentCreate,
};
